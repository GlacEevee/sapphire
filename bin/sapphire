#!/usr/bin/env ruby
# sapphire — executable entry point
# Install: sudo cp this to /usr/local/bin/sapphire && chmod +x /usr/local/bin/sapphire
# Or add the bin/ dir to your PATH

$LOAD_PATH.unshift(File.expand_path('..', __dir__))
load File.expand_path('../sapphire.rb', __dir__)
