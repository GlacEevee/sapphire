#!/usr/bin/env ruby
# sph — Sapphire Package Manager executable

this_file = File.realpath(__FILE__)
sapphire_dir = File.expand_path('../..', this_file)

$LOAD_PATH.unshift(sapphire_dir)
load File.join(sapphire_dir, 'sph.rb')
